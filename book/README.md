# The Gleam Book

The rendered version can be found at https://lpil.uk/gleam


## Build book in localhost

Install `mdbook`:

```
$ cargo install mdbook
```

and run:
```
$ make book-serve
```

in root directory.
