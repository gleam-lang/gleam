[package]
name = "erlang-abstract-format"
version = "1.0.0"
edition = "2024"

[dependencies]
# Encoding values in the erlang term format
erlang-term-format = { path = "../erlang-term-format" }
itertools.workspace = true
num-bigint.workspace = true
num-traits.workspace = true
