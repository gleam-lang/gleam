// use crate::{
//     io::memory::InMemoryFile,
//     language_server::{engine::LanguageServerEngine, progress::ProgressReporter},
//     paths::ProjectPaths,
// };

// #[test]
// fn start_your_engines() {
//     let config = Config::default();
//     let progress_reporter = ProgressReporter::new();
//     let io = InMemoryFile::new();
//     let paths = ProjectPaths::at_filesystem_root();
//     let engine = LanguageServerEngine::new(config, progress_reporter, io, paths);
// }
