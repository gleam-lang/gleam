{erl_opts, [debug_info]}.
{src_dirs, ["src", "gen/src"]}.

{profiles, [
    {test, [
        {src_dirs, ["gen/test"]}
    ]}
]}.

{pre_hooks, [{"(linux|darwin|solaris|win32)", compile, "gleam build ."}]}.

{deps, []}.
