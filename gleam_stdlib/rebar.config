{erl_opts, [debug_info]}.
{src_dirs, ["src", "gen/src"]}.

{profiles, [
    {test, [
        {src_dirs, ["src", "gen/src", "gen/test"]}
    ]}
]}.

{pre_hooks, [{compile, "gleam build ."}]}.

{deps, []}.
