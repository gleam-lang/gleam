name = "glerm"
version = "0.1.0"
description = "A terminal wrapper for Gleam"

# Fill out these fields if you intend to generate HTML documentation or publish
# your project to the Hex package manager.
licences = ["Apache-2.0"]
repository = { type = "github", user = "rawhat", repo = "glerm" }

[dependencies]
gleam_stdlib = "~> 0.28"
gleam_erlang = "~> 0.19"
gleam_otp = "~> 0.5"
gleam_community_ansi = "~> 1.1"

[dev-dependencies]
gleeunit = "~> 0.7"
