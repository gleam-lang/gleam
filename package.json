{
  "name": "gleam",
  "version": "1.0.0",
  "scripts": {
    "format": "prettier --write '**/*.{css,js}'",
    "check-format": "prettier --check '**/*.{css,js}'"
  },
  "dependencies": {
    "prettier": "^2.7.1"
  }
}
